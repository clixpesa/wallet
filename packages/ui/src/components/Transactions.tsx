import { YStack, styled, YGroup, withStaticProperties, Separator } from 'tamagui'

const TransactionsWrapper = styled(YStack, {
  boc: '$color4',
  gap: '$5',
  f: 1,
})

export const Transactions: React.FC<TransactionsProps> = (props) => {
  return <YStack>{/* */}</YStack>
}
